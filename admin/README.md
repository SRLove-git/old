# 岁悦里俱乐部 · 运营后台（Web）

基于 Vue 3 + Vite，对接 `/api` 后端接口。

## 启动

```bash
cd admin
npm install
npm run dev
```

开发服务器会把 `/api` 代理到 `http://127.0.0.1:3000`（需先启动 `server/` 后端）。

打开浏览器访问 Vite 输出的地址（默认 `http://localhost:5173`）。

## 模块

- 数据看板：活动/订单/客户/主理人统计、订单状态分布、累计佣金
- 活动管理：列表、新建、编辑、删除
- 订单管理：支付、取消、推进状态、退款
- 主理人审核：通过 / 拒绝（填原因）
- 主理人管理：冻结 / 解冻 / 清退
- 客户归属：解绑 / 修改归属
- 佣金管理：一键转可结算、单笔调整
- 提现审核：打款 / 拒绝
- 规则配置：全局分佣比例、最低提现、每月提现次数
